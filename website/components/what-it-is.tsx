export function WhatItIs() {
  return (
    <section className="mx-auto max-w-5xl px-4 py-16 sm:px-6 lg:px-8">
      <h2 className="font-bold text-2xl sm:text-3xl">What is ♪ GitJazz?</h2>
      <p className="mt-4 max-w-3xl text-jazz-gray leading-relaxed">
        A CLI that replaces noisy raw git output with guided, AI-enhanced
        terminal workflows. Stage, commit, push, pull, and merge — all with AI
        that writes your commit messages and resolves merge conflicts
        automatically.
      </p>
    </section>
  );
}
